<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.github.pocmo.sensordashboard_old">

    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <activity
            android:name="com.github.pocmo.sensordashboard_old.MainActivity"
            android:label="@string/app_name">
        </activity>


        <activity
            android:name="com.github.pocmo.sensordashboard_old.AboutActivity"
            android:label="@string/action_about">
        </activity>

        <activity
            android:name="com.github.pocmo.sensordashboard_old.ui.ExportActivity"
            android:parentActivityName="com.github.pocmo.sensordashboard_old.MainActivity"
            android:label="@string/action_export">
        </activity>


        <service android:name="com.github.pocmo.sensordashboard_old.SensorReceiverService">
            <intent-filter>
                <action
                    android:name="com.google.android.gms.wearable.BIND_LISTENER"/>
            </intent-filter>
        </service>
    </application>

</manifest>
